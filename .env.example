# LifeQuest stores all habit data in the browser (localStorage), so the app
# runs perfectly with NO environment variables at all.
#
# DATABASE_URL is optional and only used by the /api/health endpoint that
# ships with the starter template. Leave it unset when deploying to Vercel.
# DATABASE_URL=postgresql://user:password@host:5432/database
